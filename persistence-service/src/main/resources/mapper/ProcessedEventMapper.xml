<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edrs.persistence.mapper.ProcessedEventMapper">

    <resultMap id="ProcessedEventResultMap" type="com.edrs.persistence.entity.ProcessedEvent">
        <id property="id" column="id"/>
        <result property="eventId" column="event_id" typeHandler="org.apache.ibatis.type.UUIDTypeHandler"/>
        <result property="correlationId" column="correlation_id" typeHandler="org.apache.ibatis.type.UUIDTypeHandler"/>
        <result property="eventType" column="event_type"/>
        <result property="processedAt" column="processed_at"/>
        <result property="handlerService" column="handler_service"/>
    </resultMap>

    <insert id="insert" parameterType="com.edrs.persistence.entity.ProcessedEvent">
        INSERT INTO processed_events (event_id, correlation_id, event_type, processed_at, handler_service)
        VALUES (#{eventId,typeHandler=org.apache.ibatis.type.UUIDTypeHandler},
                #{correlationId,typeHandler=org.apache.ibatis.type.UUIDTypeHandler},
                #{eventType},
                COALESCE(#{processedAt}, CURRENT_TIMESTAMP),
                #{handlerService})
    </insert>

    <select id="findByEventId" parameterType="java.util.UUID" resultMap="ProcessedEventResultMap">
        SELECT id, event_id, correlation_id, event_type, processed_at, handler_service
        FROM processed_events
        WHERE event_id = #{eventId,typeHandler=org.apache.ibatis.type.UUIDTypeHandler}
    </select>

    <select id="existsByEventId" parameterType="java.util.UUID" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM processed_events WHERE event_id = #{eventId,typeHandler=org.apache.ibatis.type.UUIDTypeHandler})
    </select>

</mapper>
